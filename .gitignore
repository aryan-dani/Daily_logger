# VS Code specific
/.vscode

# Node dependencies
/node_modules

# Environment variables
.env
.env.*.local

# Production environment variables (if applicable, Render uses its own system)
# .env.production

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
server/logs.json

# Session files
server/sessions/

# Sensitive data / Databases (keep out of Git)
server/users.json
server/logs_db.json
server/email-config.js # Already ignored, good.

# Optional: Ignore OS generated files
.DS_Store
Thumbs.db